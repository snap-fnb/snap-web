{{#if isLoading}}
  <div class="loading">
    <i class="fa fa-spinner fa-spin fa-2x fa-fw"></i>
    <span class="sr-only">Loading...</span>
  </div>
{{else}}
  {{#each-in results as |key value|}}
    <div class="result-group">
      <h3>{{key}}</h3>
      <ul>
        {{#each value as |resultItem|}}
          <li {{action 'chooseItem' resultItem}}>
            {{resultItem.desc}}
          </li>
        {{/each}}
      </ul>
    </div>
  {{/each-in}}
{{/if}}
